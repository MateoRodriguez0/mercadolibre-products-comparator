spring:
  application:
    name: servicio-prueba
  cloud:
    config:
     # uri:
      #- http://localhost:8887
      discovery:
        enabled: true
        service-id: config-server
        username: Productcomparatorserviceconfig
        password: client12345
  config:
    import: 'optional:configserver:'
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    instance-id: servicio-prueba
    